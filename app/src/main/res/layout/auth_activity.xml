<android.support.design.widget.CoordinatorLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    tools:context="cl.uc.saludestudiantiluc.auth.AuthActivity"
    android:id="@+id/coordinator_layout"
    >

    <include layout="@layout/main_background_image"/>

    <ProgressBar
        style="?android:attr/progressBarStyleLarge"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:id="@+id/auth_progress_bar"
        android:layout_gravity="center"
        />

    <ScrollView
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:id="@+id/auth_fragment_container"
        android:layout_marginTop="@dimen/card_view_text_margin_top_bottom"
        android:layout_marginBottom="@dimen/card_view_text_margin_top_bottom"
        />
</android.support.design.widget.CoordinatorLayout>